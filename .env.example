# Copy to .env.local — Next.js loads it at dev-server startup (restart after edits).
# Deployed Modal backend for the diagnostic pipeline (required by /api/analyze proxy).
NEXT_PUBLIC_API_URL=https://mohamedymay7--fixation-api-fastapi-app.modal.run
# Higgsfield platform API credentials for revised-KV generation (/api/adapt).
HF_API_KEY_ID=
HF_API_KEY_SECRET=
